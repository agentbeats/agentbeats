# Development dependencies - for working with the source code
# Note: In production, just install 'agentbeats' from PyPI

# Core dependencies
a2a-sdk
uvicorn[standard]>=0.27.0
fastmcp
openai-agents
sqlalchemy
toml

# FastAPI and related
fastapi>=0.104.0
pydantic>=2.0.0

# Authentication and OAuth
supabase
PyJWT
python-dotenv

# HTTP client
httpx

# Async support
asyncio

# Logging and utilities
python-multipart

# Docker and SSH testing (for battle royale scenario)
paramiko>=3.4.0
requests>=2.31.0
asyncio-mqtt>=0.16.1
paho-mqtt>=1.6.1

# Development only - do not include agentbeats itself when developing
# agentbeats  # <-- commented out for development

# Sec-Bench Huggingface Dataset
datasets