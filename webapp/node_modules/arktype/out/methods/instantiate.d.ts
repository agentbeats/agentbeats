import type { anyOrNever, array } from "@ark/util";
import type { ArrayType } from "arktype/internal/methods/array.ts";
import type { BaseType } from "arktype/internal/methods/base.ts";
import type { DateType } from "arktype/internal/methods/date.ts";
import type { NumberType } from "arktype/internal/methods/number.ts";
import type { ObjectType } from "arktype/internal/methods/object.ts";
import type { StringType } from "arktype/internal/methods/string.ts";
export type instantiateType<t, $> = [
    t
] extends [anyOrNever] ? BaseType<t, $> : [t] extends [object] ? [
    t
] extends [array] ? ArrayType<t, $> : [t] extends [Date] ? DateType<t, $> : ObjectType<t, $> : [t] extends [string] ? StringType<t, $> : [t] extends [number] ? NumberType<t, $> : BaseType<t, $>;
