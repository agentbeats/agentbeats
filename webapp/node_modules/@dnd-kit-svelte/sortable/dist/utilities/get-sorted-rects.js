export function getSortedRects(items, rects) {
    return items.reduce((accumulator, id, index) => {
        if (!rects)
            return accumulator;
        const rect = rects.get(id);
        if (rect) {
            accumulator[index] = rect;
        }
        return accumulator;
    }, Array(items.length));
}
