import { untrack } from 'svelte';
export function useLazyMemo(fn) {
    let current = $state();
    $effect(() => {
        current = fn(untrack(() => current));
    });
    return {
        get current() {
            return current;
        },
    };
}
