export function useInterval() {
    let intervalRef = null;
    const set = (listener, duration) => {
        intervalRef = setInterval(listener, duration);
    };
    const clear = () => {
        if (intervalRef !== null) {
            clearInterval(intervalRef);
            intervalRef = null;
        }
    };
    return [set, clear];
}
