const ids = {};
export function useUniqueId(prefix, value) {
    if (value) {
        return value;
    }
    const id = ids[prefix] == null ? 0 : ids[prefix] + 1;
    ids[prefix] = id;
    return `${prefix}-${id}`;
}
