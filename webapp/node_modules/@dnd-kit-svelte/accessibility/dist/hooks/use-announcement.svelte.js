export function useAnnouncement() {
    let announcement = $state('');
    const announce = (value) => {
        if (value != null) {
            announcement = value;
        }
    };
    return {
        announce,
        announcement: {
            get current() {
                return announcement;
            },
        },
    };
}
