import type { DraggableNode } from '../../../store/types.js';
import type { MeasuringFunction } from '../types.js';
import type { ClientRect } from '../../../types/index.js';
interface Options {
    activeNode: DraggableNode | null | undefined;
    config: boolean | {
        x: boolean;
        y: boolean;
    } | undefined;
    initialRect: ClientRect | null;
    measure: MeasuringFunction;
}
export declare function useLayoutShiftScrollCompensation(optionsFn: () => Options): void;
export {};
