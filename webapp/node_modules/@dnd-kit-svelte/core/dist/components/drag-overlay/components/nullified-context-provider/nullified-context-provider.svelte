<script lang="ts">import { ActiveDraggableContextKey } from "../../../index.js";
import { InternalContextKey, defaultInternalContext } from "../../../../store/index.js";
import { setContext } from "svelte";
let { children } = $props();
const defaultTransform = {
  x: 0,
  y: 0,
  scaleX: 1,
  scaleY: 1
};
setContext(InternalContextKey, () => defaultInternalContext);
setContext(ActiveDraggableContextKey, () => defaultTransform);
</script>

{@render children()}
