import { AbstractPointerSensor, type PointerSensorProps, type PointerSensorOptions } from '../pointer/index.js';
import type { SensorProps } from '../types.js';
export interface TouchSensorOptions extends PointerSensorOptions {
}
export type TouchSensorProps = SensorProps<TouchSensorOptions>;
export declare class TouchSensor extends AbstractPointerSensor {
    constructor(props: PointerSensorProps);
    static activators: {
        eventName: "ontouchstart";
        handler: (event: TouchEvent, { onActivation }: TouchSensorOptions) => boolean;
    }[];
    static setup(): () => void;
}
