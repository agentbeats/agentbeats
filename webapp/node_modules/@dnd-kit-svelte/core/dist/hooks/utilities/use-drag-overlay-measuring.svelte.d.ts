import type { ClientRect } from '../../types/rect.js';
interface Arguments {
    measureFn: () => (element: HTMLElement) => ClientRect;
}
export declare function useDragOverlayMeasuring({ measureFn }: Arguments): {
    readonly nodeRef: HTMLElement | null;
    readonly rect: ClientRect | null;
    setRef: (element: HTMLElement | null) => void;
};
export {};
