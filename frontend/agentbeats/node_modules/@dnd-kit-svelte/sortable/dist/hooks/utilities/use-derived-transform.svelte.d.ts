import { type ClientRect } from '@dnd-kit-svelte/core';
import type { Transform } from '@dnd-kit-svelte/utilities';
interface Arguments {
    rect: ClientRect | null;
    disabled: boolean;
    index: number;
    node: HTMLElement | null;
}
export declare function useDerivedTransform(argsFn: () => Arguments): {
    readonly current: Transform | null;
};
export {};
