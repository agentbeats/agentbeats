import { getWindowClientRect } from '../../utilities/rect/get-window-client-rect.js';
export function useWindowRect(elementFn) {
    const element = $derived(elementFn());
    return {
        get current() {
            return element ? getWindowClientRect(element) : null;
        },
    };
}
