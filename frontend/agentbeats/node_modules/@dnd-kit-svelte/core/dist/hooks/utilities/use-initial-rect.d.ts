import type { ClientRect } from '../../types/index.js';
export declare function useInitialRect(node: () => HTMLElement | null, measure: () => (node: HTMLElement) => ClientRect): {
    readonly current: ClientRect | null;
};
