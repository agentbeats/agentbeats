import type { DraggableNodes } from '../../store/types.js';
import type { UniqueIdentifier } from '../../types/other.js';
export declare function useCachedNode(args: () => [draggableNodes: DraggableNodes, id: UniqueIdentifier | null]): {
    readonly current: HTMLElement | null;
};
