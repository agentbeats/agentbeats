import type { ClientRect } from '../../types/index.js';
export declare function useRects(args: () => [elements: Element[], measure?: (element: Element) => ClientRect]): {
    readonly current: ClientRect[];
};
