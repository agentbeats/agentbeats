import { box } from 'svelte-toolbelt';
export function useNodeRef(onChange) {
    let node = $state(null);
    const setNodeRef = (element) => {
        if (element !== node) {
            onChange?.(element, node);
        }
        node = element;
    };
    return [box.with(() => node, setNodeRef), setNodeRef];
}
