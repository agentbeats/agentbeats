export function useState(initial) {
    let value = $state(initial);
    const read = () => value;
    const write = (val) => {
        if (typeof val === 'function') {
            value = val(value);
        }
        else {
            value = val;
        }
    };
    return [read, write];
}
