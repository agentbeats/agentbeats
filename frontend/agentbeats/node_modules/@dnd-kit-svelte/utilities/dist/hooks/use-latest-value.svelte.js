export function useLatestValue(value, dependencies) {
    let valueRef = $state(value);
    $effect(() => {
        dependencies(); // trigger re-run
        if (valueRef !== value) {
            valueRef = value;
        }
    });
    return {
        get current() {
            return valueRef;
        },
    };
}
